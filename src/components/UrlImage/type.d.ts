interface UrlImageStyleTypes {
  border?: boolean;
  fit?: boolean;
  height?: Dimensions;
  rounded?: boolean;
  url: string;
  width?: Dimensions
}

interface UrlImageProps extends UrlImageStyleTypes { };
